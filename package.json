{
  "name": "fetch_with_checks",
  "version": "1.0.0",
  "description": "A wrapper for fetch() with safety checks",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --target=chrome58,firefox57 --outfile=out.js",
    "test": "npx esbuild test/suite.ts --outfile=test/suite.js --bundle --platform=node && mocha test/suite.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.2.22",
    "@types/mocha": "^9.0.0",
    "@types/node": "^16.11.10",
    "chai": "^4.3.4",
    "esbuild": "^0.13.15",
    "mocha": "^9.1.3",
    "node-fetch": "^3.1.0"
  }
}
